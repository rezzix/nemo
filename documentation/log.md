# Nemo Knowledge Base — Update Log

## 2026-06-30
* **Update**: Rewrote the bundle-root `index.md` to reflect the current folder structure. Each section now maps to a top-level directory (overview, modules, security, implementation, testing, references) and lists its key files directly, replacing the previous version's mixed heading styles and stale references.
* **Update**: Removed the "Execution strategy" section (CI pipeline, implementation-priority table, coverage-targets table) from `testing/test-plan.md`. The per-module `module-testplan.md` files already contain the specific test cases; the global plan now focuses on shared strategy, infrastructure, and test data. Restructured `testing/index.md` to list per-module test plans first, global strategy second.

## 2026-06-27
* **Creation**: Initialized the OKF v0.1 knowledge base under `documentation/`. Built the full directory tree: bundle root index and log, overview, per-module backend concepts, architecture-level frontend concepts, data model, API, security, cross-cutting concerns, testing, and references. Content distilled from the legacy `docs/` vault and the live codebase, corrected to the current state (React 19 + Zustand 5; finance/bank/reconciliation modules present).
* **Update**: Renamed the remaining stale `issue`→`task` references across the legacy `docs/`, `README.md`, the Postman collection, and the live code (`Task.java` column `issue_key`→`task_key`, a `PmoService` comment, and a dead `SummaryTab` tab id). Preserved the RAID `Issue` category (enum, union literals, `raid_item` CHECK constraint, "Risks, Assumptions, Issues, Dependencies" prose), the English verb "issues" (e.g. "issues a JSESSIONID"), the "rounding issues" idiom, and GitHub ticket references (`issue #222/#223/#224/#225`). Drift notes pointing at the old `issue/` package were removed from this bundle.
* **Update**: Reorganized `modules/` from flat files into a hierarchical tree of group folders — `cross-cutting/`, `identity/`, `delivery/`, `commercial/`, `finance/`, `hr/`, `timetracking/`, `content/` — mirroring the existing index groupings. Split `timetracking` into a top module with submodules (`time-log`, `timesheet`, `user-rate`, `time-report`). Added a new `hr/` group with `leave` plus a planned `evaluation` stub (no backend package yet). Rewrote all bundle-wide `/modules/*` cross-links (114 replacements across 43 files); `modules/index.md` regenerated to the grouped structure.
* **Update**: Slimmed frontmatter for easier maintenance. Removed the four redundant fields — `description` (duplicated each body's intro), `title` (equal to the H1 heading in 60/64 files), `timestamp` (uniform `2026-06-27` on every file; history lives in this log), and `tags` (consumed by nothing in the bundle) — from all 65 frontmatter files (257 field removals). Each concept now carries only `type` (OKF-required) and `resource` (unique code-path provenance); the bundle-root `index.md` keeps only `okf_version`. OKF v0.1 conformance is preserved — §9 requires only `type`, and the spec says consumers must not reject bundles for missing optional fields.
* **Update**: De-duplicated entity/field details so the `data/` concepts are the single source of truth. Removed the entity field/name lists from the modules that restated them — `task` (`Task`/`Comment` fields), `user` (`User` field list), `project`, `phase`, `leave`, `timetracking` (entity names), and `attachment` (field list) — and pointed each to its data concept in the Cross-references section (e.g. task → `Project delivery entities`, user → `Core entities`, leave/timetracking/attachment → `Config and ops entities`). Enriched `data/config-and-ops-entities.md`'s `Attachment` row with the field list (filename, path, contentType, size) that previously lived only in the attachment module, so no detail was lost. `client-module` still names `Client`/`ClientContact` because no data concept covers clients yet (gap to address separately).* **Update**: Dissolved the root `data/` folder and gave each module group its own entity file. Every group folder now has a `module-entities.md` (`type: Schema`) holding the entity field tables and a `mermaid erDiagram` for all of its submodules: `identity` (Company, User), `cross-cutting` (OrganizationConfig, TaskType, TaskStatus, PublicHoliday, AuditLog, ActivityLog), `delivery` (program/project/task/sprint/phase/PMO), `commercial` (Client, ClientContact, ProjectExpense, ProjectPayment), `finance` (BankAccount, BankStatement, BankTransaction), `hr` (LeaveRequest, LeaveEntitlement, planned Evaluation), `timetracking` (TimeLog, UserRate, Timesheet), `content` (WikiPage, Attachment, Asset, Location). This also closes the earlier client gap (`Client`/`ClientContact` now have a home). Moved `data/database-schema.md` → `overview/database-schema.md` (rewritten to index the eight module-entities files instead of the old data concepts) and repointed the 18 inbound `/data/*.md` links plus the root `index.md` Data-model entry. Submodules that own entities but had no entity link now cross-reference their group's `module-entities.md` (program, client, common, config, documentation). Verified: no `/data/` refs remain, 0 broken bundle-absolute links, OKF §9 conformance holds, all 8 mermaid fences balanced.
* **Update**: Split the test plan into per-module `module-testplan.md` files, mirroring the `module-entities.md` pattern. Compiled each from the legacy `docs/test-plan.md`: `cross-cutting` (CaptchaService, AuthHelper, auth/RBAC/org-config/task-type/audit/error endpoints, frontend infra — API client, auth store, hooks, guards, LoginPage, Sidebar, TopBar, plus auth/RBAC/edge E2E), `identity` (user/company endpoints, admin-users-tab frontend, multi-tenancy E2E), `delivery` (program/project/task/sprint/PMO-RAID endpoints, Project/Task/Pmo service + mapper unit tests, project-tab-visibility frontend, CRUD E2E), `timetracking` (time-log service/mapper, time-tracking endpoints, log-time E2E), and `content` (WikiPageService, wiki endpoints, wiki/mermaid/upload E2E). The `commercial`, `finance`, and `hr` groups postdate the legacy plan, so their `module-testplan.md` files are planned stubs listing what to cover. Expanded `testing/test-plan.md` to hold the shared strategy the module plans cross-reference (four-level strategy, backend infra + base class, test data, execution priority/coverage targets, maintenance) and indexed the eight per-module plans; `testing/index.md` and `modules/index.md` updated for discoverability. Each module test plan is `type: Playbook`, `resource: docs/test-plan.md`, and links its `module-entities.md`, the global test plan, and the Postman collection. Verified: OKF §9 conformance holds, 0 broken bundle-absolute links, all 8 files present.
* **Update**: Grouped the implementation-level concepts under a new `implementation/` folder and added an `architecture/` subfolder compiled from the legacy `docs/`. Moved `api/`, `frontend/`, and `cross-cutting/` into `implementation/` and rewrote all 56 inbound bundle-absolute doc links (`/api/`, `/frontend/`, `/cross-cutting/` → `/implementation/<x>/`) across 29 files; the moved folders' internal cross-links were rewritten in the same pass. Restructured the bundle-root `index.md` to a single `# Implementation` section (architecture, frontend, API, cross-cutting) and added `implementation/index.md` as the landing page. Compiled `implementation/architecture/` from `docs/architecture.md` and `docs/uml-diagrams.md`: `system-architecture.md` (`type: System`) gives the integrated backend/frontend/security/real-time/storage/deployment/API-design view with diagrams, cross-linking the existing overview/security/cross-cutting/api/frontend concepts rather than duplicating them; `uml-diagrams.md` (`type: System`) preserves all eight legacy Mermaid diagrams — domain class diagram, per-role use cases, task lifecycle state, auth/Kanban/time-tracking sequences, backend package component diagram, and the role-based access overview — each cross-linked to its module/security concept. Verified: 0 stale `/api/`·`/frontend/`·`/cross-cutting/` links, 0 broken bundle-absolute links, OKF §9 conformance holds, code fences balanced.
* **Update**: Shipped the remaining legacy `docs/` content into the bundle, dropping duplicates and obvious/meta content. `docs/getting-started.md` → new `overview/getting-started.md` (`type: System`): prerequisites, running the backend/frontend, run modes (`dev`/`demo`/`prod`), version+build timestamp, default seed data, H2 console, DB reset, and the PostgreSQL production profile — the duplicated project-structure, API-quick-reference, Postman/Newman, and design-docs-table sections were dropped in favor of cross-links (`monorepo-structure`, `api`, `testing`). Moved the database-portability note (reserved-word `key_`/`order_`/… suffixes, `app_user`/`task_comment` table names) into `overview/database-schema.md` where it belongs. `docs/pmo-step1-domain-model.md` was almost entirely duplicate of the now-existing `module-entities` and `uml-diagrams`; its only unique value — the EVM metric formulas (EV/PV/AC/CV/SV/CPI/SPI) and the 5×5 RAID risk matrix + risk levels — was folded into `modules/delivery/pmo-module.md` (previously a thin stub), with the duplicate model-summary, files-list, and class-diagram parts discarded. `docs/gen_prompt.md` (a meta "recreate-from-scratch" generation prompt, not application knowledge) was discarded. The legacy `docs/` set is now fully represented in the bundle. Verified: 0 broken bundle-absolute links, OKF §9 conformance holds, code fences balanced, `getting-started` wired into `overview/index.md` and the bundle-root `index.md`.
* **Update**: Added `implementation/architecture/frontend-architecture.md` (`type: System`): a one-page frontend summary distilling the layer diagram, tech stack, directory layout, routing/guards, API client layer, state management (Zustand stores + hooks), and dashboards/reports from the five frontend concept documents, with cross-links into each. Trimmed the Frontend section in `system-architecture.md` to a brief pointer (avoiding duplication). Wired the new file into `architecture/index.md`, `frontend/index.md`, and the system-architecture cross-ref. Verified: 0 broken links, OKF conformant.
* **Update**: Deleted `implementation/frontend/` folder (the five concept documents are now summarized in `implementation/architecture/frontend-architecture.md`). Rewired all 10 inbound links that previously pointed to `/implementation/frontend/` files — 4 inside `frontend-architecture.md` itself (routing-and-guards, api-client-layer, state-management, dashboards) converted to in-file references, and 6 across the bundle (tech-stack, features-and-access, getting-started, documentation-module, reports-module, rest-conventions) retargeted to `/implementation/architecture/frontend-architecture.md`. Updated `implementation/index.md` and the bundle-root `index.md` to reference the new location. Verified: 0 `/implementation/frontend/` links remain, 0 broken bundle-absolute links.
