nemo:
  devmode: false

spring:
  datasource:
    url: ${NEMO_DB_URL}
    driver-class-name: org.postgresql.Driver
    username: ${NEMO_DB_USERNAME}
    password: ${NEMO_DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  h2:
    console:
      enabled: false

  sql:
    init:
      mode: never